import { RoundToIntegerPipe } from './round-to-integer.pipe';

describe('RoundToIntegerPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundToIntegerPipe();
    expect(pipe).toBeTruthy();
  });
});
