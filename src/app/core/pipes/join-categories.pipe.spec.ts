import { JoinCategoriesPipe } from './join-categories.pipe';

describe('JoinCategoriesPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinCategoriesPipe();
    expect(pipe).toBeTruthy();
  });
});
